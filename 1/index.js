console.log("teste");

/*
Um programa que altere todas as classes de um determinado lista(LI) em html
adicionando o número da iteração no valor de cada li.
*/
const lista = document.getElementsByClassName("teste");

/*
for (let i = 0; i < lista.length; i++) {
  const pos = i + 1;
  lista[i].innerText = `lista na posicao ${pos}`;
}
*/
